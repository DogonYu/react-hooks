import React, { useState, useRef, useEffect } from 'react';

const App = () => {
  return (
    <div className="App">
      <button onClick={triggerFull}>Make fullscreen</button>
    </div>
  );
};

export default App;
